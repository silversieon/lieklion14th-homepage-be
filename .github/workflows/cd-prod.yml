name: CD - Production

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout repository (with submodules)
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GIT_ACTION_TOKEN }}
          submodules: true

      - name: Docker Hub Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker Build & Push
        run: |
          echo "🔵 [PROD] Building Docker image"
          docker build -f ./docker/Dockerfile \
            -t ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_REPO }}:latest \
            -t ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_REPO }}:prod-${{ github.sha }} .

          echo "🔵 [PROD] Pushing Docker image"
          docker push ${{ secrets.DOCKER_USERNAME }}/${{ secrets.PROD_DOCKER_REPO }}:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/${{ secrets.PROD_DOCKER_REPO }}:latest-${{ github.sha }}

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment: production

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Copy docker-compose.yml to Server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.PROD_SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.PROD_SSH_PRIVATE_KEY }}
          source: "docker/docker-compose.yml,docker/docker-compose-prod.yml"
          target: "~/app"

      - name: Deploy on Production Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.PROD_SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.PROD_SSH_PRIVATE_KEY }}
          script: |
            cd ~/app/docker

            echo "🔵 [PROD] .env 생성 중..."
            printf "${{ secrets.PROD_ENV_FILE }}" > .env

            echo "🔵 [PROD] 최신 이미지 pull (app만)"
            docker compose -f docker-compose.yml -f docker-compose-prod.yml pull app

            echo "🔵 [PROD] 앱 컨테이너만 재기동"
            docker compose -f docker-compose.yml -f docker-compose-prod.yml up -d --no-deps app

            echo "🔵 [PROD] 사용하지 않는 이미지 정리"
            docker image prune -f

            echo "✅ [PROD] 배포 완료!"